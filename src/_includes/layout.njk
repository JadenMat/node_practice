<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or "Untitled Page" }}</title>

  {% if stylesheet %}
    <link rel="stylesheet" href="{{ pathPrefix }}styles/{{ stylesheet }}">
  {% else %}
    <link rel="stylesheet" href="{{ pathPrefix }}styles/index.css">
  {% endif %}

  {# Optional preloaded images #}
  {% if preloadImages %}
    {% for img in preloadImages %}
      <link rel="preload" href="{{ pathPrefix }}{{ img }}" as="image">
    {% endfor %}
  {% endif %}
</head>

<body>
  <div class="grid-container">
    <header class="header">{% include "nav.njk" %}</header>
    <main class="content">
      {% include "animation.njk" %}
      {{ content | safe }}
    </main>
    <section class="info"><p>{{ infoText }}</p></section>
    <footer class="footer">{% include "footer.njk" %}</footer>
  </div>
</body>
</html>